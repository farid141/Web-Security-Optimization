# K6

Tools untuk performance test, berupa:

- load
- stress
- spike
- soak

bagian script k6:

- export const options = {}
- export function setup() {} setup sebelum testing (dijalankan sekali)
- export default function() {} skenario testing (infinite loop selama duration dll) berikan sleep jika bukan stress test
